version: 1
frontend:
  phases:
    preBuild:
      commands:
        - nvm install 22.18.0
        - npm install -g npm@latest
        - npm ci
    build:
      commands:
        - npm run build:web
  artifacts:
    baseDirectory: dist/
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
      - .expo/**/*
  customHeaders:
    - pattern: '**/*'
      headers:
        - key: 'Cache-Control'
          value: 'public, max-age=0, must-revalidate'
