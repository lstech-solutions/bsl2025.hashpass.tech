{
  // Cloudflare Pages/Workers configuration
  // This file is used by Wrangler for Cloudflare deployments
  // It does not affect Netlify or Amplify builds (they use their own config files)
  //
  // IMPORTANT: This config is for Cloudflare Pages (not Workers)
  // To deploy: npx wrangler pages deploy ./dist/client
  // NOT: npx wrangler deploy (that's for Workers)
  //
  "name": "hashpass",
  "compatibility_date": "2025-11-16",
  "assets": {
    "directory": "./dist/client"
  }
}

